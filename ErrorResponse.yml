components:
    schemas:
        ErrorResponse:
            type: object
            properties: {data: {type: array, items: {$ref: '#/components/schemas/ErrorMessage'}}}
        ErrorMessage:
            type: object
            properties: {message: {type: string, example: 'Server had an Error'}, context: {type: object}}
